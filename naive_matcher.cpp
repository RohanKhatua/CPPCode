#include <bits/stdc++.h>
using namespace std;

int main(){

    cout<<"Enter String and Pattern : ";
    string s,p;
    cin>>s>>p;

    for (int i=0;i<s.length()-p.length();i++){
        
    }

    return 0;
}